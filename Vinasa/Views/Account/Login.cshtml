@model Vinasa.Models.AdminAccountModels
@{
    ViewBag.Title = "Đăng nhập";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>

    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>QUẢN LÝ HỘI VIÊN VINASA - ĐĂNG NHẬP</title>

    <!-- Custom fonts for this template-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@200;300;400;500&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <link href="~/css/style.css" rel="stylesheet" />

    <link rel="icon" href="~/img/iconVinasa (2).png" type="image/png">

    <!-- Custom styles for this template-->

</head>
<body class="bg-gradient-primary ">
    <div class="container-lg">
        <div class="login-form">
            <div class="left-content">
                <h2 class="heading-left">
                    <img src="~/img/big-catalog-16062358931 (1).png" alt="" class="logo" />
            </div>
            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = " user form-horizontal", role = "form" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group" class="right-content">
                    <h2 class="heading-right">ĐĂNG NHẬP ĐỂ TIẾP TỤC</h2>
                    <div class="input-wrapper">
                        <input class="ip-form" type="text" id="email" required @Html.TextBoxFor(m => m.Email)
                        <label class="lb-form" for="username">Email</label>
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                    </div>
                    <div class="input-wrapper">
                        <input class="ip-form" type="password" id="password" required @Html.PasswordFor(m => m.MatKhau)
                        <label class="lb-form" for="password">Mật Khẩu</label>
                        @Html.ValidationMessageFor(m => m.MatKhau, "", new { @class = "text-danger" })
                    </div>
                    <input type="submit" value="Đăng nhập" class="button-login" />
                </div>
            }
        </div>
    </div>

    @if (ViewBag.Message != null)
    {
        <script type="text/javascript">
        window.onload = function () {
            alert("@ViewBag.Message");
        };
        </script>
    }
    <!-- Bootstrap core JavaScript-->
    <script src="@Url.Content("~/vendor/jquery/jquery.min.js")"></script>
    <script src="@Url.Content("~/vendor/bootstrap/js/bootstrap.bundle.min.js")"></script>

    <!-- Core plugin JavaScript-->
    <script src="@Url.Content("~/vendor/jquery-easing/jquery.easing.min.js")"></script>

    <!-- Custom scripts for all pages-->
    <script src="@Url.Content("~/js/sb-admin-2.min.js")"></script>
</body>

</html>
@*@RenderSection("scripts", required: false)*@
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}